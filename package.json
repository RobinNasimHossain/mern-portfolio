{
  "name": "mern-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Root package for Vercel monorepo deploy (frontend in client/, Express API wrapped as a serverless function in api/).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "vercel-build": "cd client && npm install --include=dev && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.1",
    "morgan": "^1.10.0",
    "slugify": "^1.6.6",
    "zod": "^3.23.8"
  }
}
